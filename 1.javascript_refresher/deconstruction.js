const user = {
    name: "Ed",
    lastName: "Dev",
    age: 20
};

const { name, lastName, age } = user
console.log(lastName)

const numbers = [1, 2, 3, 4, 5]
const [yol, lo] = numbers

console.log(yol, lo)